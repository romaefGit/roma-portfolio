import { AnimateHoverDirective } from './animate-hover.directive';

describe('AnimateHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateHoverDirective();
    expect(directive).toBeTruthy();
  });
});
