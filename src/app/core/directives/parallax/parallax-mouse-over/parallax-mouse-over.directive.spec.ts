import { ParallaxMouseOverDirective } from './parallax-mouse-over.directive';

describe('ParallaxMouseOverDirective', () => {
  it('should create an instance', () => {
    // const directive = new ParallaxMouseOverDirective();
    // expect(directive).toBeTruthy();
  });
});
