import { LoadedDirective } from './loaded.directive';

describe('LoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadedDirective();
    expect(directive).toBeTruthy();
  });
});
